<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:views="clr-namespace:ETS2LA.UI.Views.Settings"
             xmlns:husk="https://github.com/d3ara1n/Huskui.Avalonia"
             x:Class="ETS2LA.UI.Views.Settings.DisplaySettings"
             x:DataType="views:DisplaySettings">
    <StackPanel>
        <Grid ColumnDefinitions="450,*">
        <StackPanel Grid.Column="0" Spacing="24" Margin="20">

            <StackPanel Spacing="10">
                <StackPanel Spacing="3">
                    <TextBlock Text="Display Units" />
                    <TextBlock Text="Which units should be used for displaying values?" FontSize="12" Classes="Description" />
                </StackPanel>
                <TabStrip ItemsSource="{Binding DisplayUnitOptions}" 
                        SelectedIndex="{Binding SelectedDisplayUnitOption}" 
                        SelectionChanged="OnDisplayUnitChanged"
                        AutomationProperties.Name="Displayed Units">

                    <TabStrip.Styles>
                        <Style Selector="TabStripItem" x:DataType="views:TabStripItemHandler">
                            <Setter Property="AutomationProperties.Name" Value="{Binding AutomationName}" />
                        </Style>
                    </TabStrip.Styles>

                    <TabStrip.ItemTemplate>
                        <DataTemplate x:DataType="views:TabStripItemHandler">
                            <Border Width="80" Height="24" VerticalAlignment="Center" HorizontalAlignment="Center">
                                <TextBlock Text="{Binding Header}" FontSize="14" VerticalAlignment="Center" HorizontalAlignment="Center" />
                            </Border>
                        </DataTemplate>
                    </TabStrip.ItemTemplate>
                </TabStrip>
                <StackPanel IsVisible="{Binding IsUsingScientificUnits}" Spacing="3">
                    <TextBlock Text="Has only slight differences to metric units, for example instead of 'bar' (Atmospheres), scientific units use 'Pa' (Pascals)." FontSize="12" />
                </StackPanel>
            </StackPanel>

            <Separator />

            <StackPanel Spacing="10">
                <Grid ColumnDefinitions="*, Auto">
                    <TextBlock Text="Support Multiple Viewports" VerticalAlignment="Center" />
                    <ToggleSwitch Grid.Column="1" IsChecked="{Binding SupportMultipleViewports}" Classes="primary-toggle" AutomationProperties.Name="Overlay support for multiple viewports" />
                </Grid>
                <TextBlock Text="This allows ETS2LA to render in multiple viewports, meaning you can move overlay windows to any monitor." FontSize="12" Classes="Description" />
                <TextBlock Text="You need to restart ETS2LA for this option to apply." FontSize="12" Classes="Warning" IsVisible="{Binding ChangedSupportMultipleViewports}" />
            </StackPanel>

            <StackPanel Spacing="10">
                <Grid ColumnDefinitions="*, Auto">
                    <TextBlock Text="Limit Overlay Framerate" VerticalAlignment="Center" />
                    <ToggleSwitch Grid.Column="1" IsChecked="{Binding LimitOverlayFramerate}" Classes="primary-toggle" AutomationProperties.Name="Limit overlay framerate" />
                </Grid>
                <TextBlock Text="In most situations, over half of ETS2LA's CPU use will be for the overlay. You can lower it's framerate to reduce CPU and GPU usage." FontSize="12" Classes="Description" />
            </StackPanel>

            <StackPanel Spacing="12" IsVisible="{Binding LimitOverlayFramerate}">
                <StackPanel Orientation="Horizontal" Spacing="6">
                    <TextBlock Text="Max Overlay Framerate" />
                    <TextBlock Text="{Binding ElementName=MaxFramerateSlider, Path=Value, StringFormat='{}{0:0} FPS'}" />
                </StackPanel>
                <Slider Name="MaxFramerateSlider" Minimum="1" Maximum="120" Value="{Binding MaxOverlayFramerate}" AutomationProperties.Name="Max Overlay Framerate" />
            </StackPanel>

            <Separator />

            <StackPanel Spacing="10">
                <Grid ColumnDefinitions="*, Auto">
                    <TextBlock Text="Render AR Elements" VerticalAlignment="Center" />
                    <ToggleSwitch Grid.Column="1" IsChecked="{Binding RenderAR}" Classes="primary-toggle" AutomationProperties.Name="Render AR elements" />
                </Grid>
                <TextBlock Text="We can render elements in the game world using our overlay. This will use additional CPU and GPU resources." FontSize="12" Classes="Description" />
            </StackPanel>

            <StackPanel Spacing="10">
                <Grid ColumnDefinitions="*, Auto">
                    <TextBlock Text="Simplified Graphics" VerticalAlignment="Center" />
                    <ToggleSwitch Grid.Column="1" IsChecked="{Binding SimplifiedGraphics}" Classes="primary-toggle" AutomationProperties.Name="Simplified graphics" />
                </Grid>
                <TextBlock Text="Using simplified graphics can improve performance on lower-end hardware, or fix situations in which advanced features like shaders are not supported." FontSize="12" Classes="Description" />
            </StackPanel>

            <StackPanel Spacing="10" IsVisible="{Binding RenderAR}">
                <Grid ColumnDefinitions="*, Auto">
                    <TextBlock Text="Stop Rendering When Game is Paused" VerticalAlignment="Center" />
                    <ToggleSwitch Grid.Column="1" IsChecked="{Binding DontRenderWhenPaused}" Classes="primary-toggle" AutomationProperties.Name="Stop rendering AR when game is paused" />
                </Grid>
                <TextBlock Text="When the game reports itself being paused, ETS2LA won't render AR elements." FontSize="12" Classes="Description" />
            </StackPanel>

            <StackPanel Spacing="12" IsVisible="{Binding RenderAR}">
                <StackPanel Orientation="Horizontal" Spacing="6">
                    <TextBlock Text="Max AR Rendering Distance" />
                    <TextBlock Text="{Binding ARDistanceDisplay}" />
                </StackPanel>
                <Slider Name="MaxARDistanceSlider" Minimum="50" Maximum="300" Value="{Binding MaxARDistance}" AutomationProperties.Name="Max AR Rendering Distance" />
            </StackPanel>

            <StackPanel Spacing="12" IsVisible="{Binding !RenderAR}">
                <TextBlock Text="Warning: ETS2LA relies heavily on AR elements, only disable if you really need performance." FontSize="12" Classes="Warning" />
            </StackPanel>

        </StackPanel>
        </Grid>
    </StackPanel>
</UserControl>
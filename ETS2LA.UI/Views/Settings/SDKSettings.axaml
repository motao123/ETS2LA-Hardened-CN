<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:husk="https://github.com/d3ara1n/Huskui.Avalonia"
             xmlns:views="clr-namespace:ETS2LA.UI.Views.Settings"
             x:Class="ETS2LA.UI.Views.Settings.SDKSettings"
             x:DataType="views:SDKSettings">
    <UserControl.Resources>
        <ItemsPanelTemplate x:Key="WrapPanelTemplate">
            <StackPanel />
        </ItemsPanelTemplate>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel>
            <TextBlock Text="点击卡片安装或卸载 SDK。" Classes="Description" Margin="14 7 14 7" />
            <ItemsControl x:Name="GameList" ItemsSource="{Binding Games}" ItemsPanel="{StaticResource WrapPanelTemplate}" AutomationProperties.Name="检测到的游戏列表">

                <ItemsControl.Styles>
                    <Style Selector="ContentPresenter" x:DataType="views:GameItem">
                        <Setter Property="AutomationProperties.AccessibilityView" Value="Content" />
                        
                        <Setter Property="AutomationProperties.Name" Value="{Binding AutomationName}" />
                    </Style>
                </ItemsControl.Styles>

                <ItemsControl.ItemTemplate>
                    <DataTemplate x:DataType="views:GameItem">
                        <husk:Card 
                            Padding="14"
                            Margin="10"
                            HorizontalAlignment="Stretch"
                            ClipToBounds="True"
                            Tag="{Binding}"
                            Cursor="Hand"
                            Classes.Highlight="{Binding IsSDKInstalled}"
                            PointerPressed="OnTriggerChange"

                            Focusable="True"
                            KeyDown="OnTriggerChangeKey"
                            AutomationProperties.Name="{Binding AutomationName}"
                            AutomationProperties.AccessibilityView="Content"
                        >
                            <Grid ColumnDefinitions="*, Auto, Auto" ColumnSpacing="6">
                                <StackPanel Spacing="6" Grid.Column="0">
                                    <StackPanel Spacing="6" Orientation="Horizontal">
                                        <husk:Tag Grid.Column="1" IsVisible="{Binding IsSDKInstalled}" Classes="Primary" HorizontalAlignment="Right">
                                            <TextBlock Text="已安装" FontSize="12" FontWeight="SemiBold" />
                                        </husk:Tag>
                                        <husk:Tag Grid.Column="1" IsVisible="{Binding !IsSDKInstalled}" Classes="Secondary" HorizontalAlignment="Right">
                                            <TextBlock Text="未安装" FontSize="12" FontWeight="SemiBold" />
                                        </husk:Tag>
                                        <TextBlock Text="{Binding Name}" FontSize="14" VerticalAlignment="Center" />
                                        <TextBlock Text="{Binding Version}" FontSize="12" Classes="Description" VerticalAlignment="Center" IsVisible="{Binding !IsUnknownVersion}" />
                                    </StackPanel>
                                    <TextBlock Text="{Binding Path}" Classes="Description" FontSize="12" Padding="0 0 0 4" />
                                </StackPanel>
                                <TextBox Grid.Column="1" PlaceholderText="输入版本" IsVisible="{Binding IsUnknownVersion}" Text="{Binding UpdatedVersion, Mode=TwoWay}" FontSize="12" Width="200" HorizontalAlignment="Right" />
                                <Button Grid.Column="2" Content="移除" FontSize="12" IsVisible="{Binding IsManuallyAdded}" Tag="{Binding}" Click="OnRemoveGame" VerticalAlignment="Center" AutomationProperties.Name="{Binding Name, StringFormat='Remove manually added {0}'}" />
                            </Grid>
                        </husk:Card>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <TextBlock Text="找不到游戏？可以选择游戏安装文件夹手动添加。如果看到“bin”和“licenses”文件夹，就说明选择正确。" Classes="Description" Margin="14 7 14 7" TextWrapping="Wrap" />
            <Button Content="手动添加游戏" Width="160" HorizontalAlignment="Left" Margin="14 7 14 7" Click="OnAddGameManually" AutomationProperties.Name="手动添加游戏" />
        </StackPanel>
    </ScrollViewer>
</UserControl>
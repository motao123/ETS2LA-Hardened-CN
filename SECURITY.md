# 安全说明

## 已实施的边界

- 插件下载仅接受 HTTPS，包必须提供 SHA-256；未配置可信签名者时不会假装验证签名。
- ZIP 条目拒绝绝对路径、`..`、重解析点和超出 512 MiB/2048 条目的归档。
- 插件路径和设置文件路径都必须位于各自根目录内。
- 控制融合拒绝非有限浮点值、非正权重和零总权重，并在输出线程异常时复位。
- Windows 凭据使用当前用户 DPAPI；非 Windows 不会把令牌持久化到文件。

## 使用建议

1. 只安装可审计、可复现来源的插件。
2. 不要以管理员/root 运行应用，除非正在进行受控调试。
3. 生产环境不要通过 `ETS2LA_CONFIG_DIR` 指向共享目录。
4. 不要把游戏资源、用户配置或令牌提交到源码仓库。

## 报告问题

请提供版本、平台、最小复现步骤和日志中的错误上下文；不要在公开报告中粘贴 JWT、配置文件或用户目录内容。
